<?php

  $path = '/dir0/dir1/myfile.php';
  $file = 'file1.txt';

  // Return filename
  // echo basename($path).'<br>';

  // Return filename without extension
  // echo basename($path, '.php').'<br>';

  // Return the dirname from the path
  // echo dirname($path).'<br>';

  // Check if a file exists
  // echo file_exists($file).'<br>';

  // Gets the absolute path
  // echo realpath($file).'<br>';

  // Checks to see if file is a file, not a folder
  // echo is_file($file).'<br>';

  // Check if writeable
  // echo is_writeable($file).'<br>';

  // Check if readable
  // echo is_readable($file).'<br>';

  // Get filesize in bytes
  // echo filesize($file).'<br>';

  // Create a directory
  // mkdir('testing');

  // Delete directory if empty
  // rmdir('testing');

  // Copy file
  // echo copy($file, 'file2.txt').'<br>';

  // Rename file
  // rename('file2.txt', 'myFile.txt');

  // Delete file
  // unlink('myFile.txt');

  // Write from file to string
  // echo file_get_contents($file).'<br>';

  // Write string to file (replaces)
  //echo file_put_contents($file, 'Goodbye World').'<br>';

  // $current = file_get_contents($file);
  // $current .= 'Goodbye World';
  // file_put_contents($file, $current);

  // Open file for reading
  // $handle = fopen($file, 'r');
  // $data = fread($handle, filesize($file));
  // echo $data;

  // Open file for writing
  // $handle = fopen('file2.txt', 'w');
  // $txt = "John Doe\n";
  // fwrite($handle, $txt);
  // $txt = "Steve Smith\n";
  // fwrite($handle, $txt);
  // fclose($handle);






















?>
